#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
	string s;
	cin >> s;
	if (s[s.size() - 3] == 's'}
		if (s[s.size() - 2] == 'a'}
			if (s[s.size() - 1] == 'n'}
				cout << "Yes" << endl;
				return (0);
	cout << "No" << endl;
}

