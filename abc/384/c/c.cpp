#include <bits/stdc++.h>
using namespace std;

struct ScoreKey {
	int score;
	char key;
}


int main() {
	vector<ScoreKey> set;

	cin >> set.score(key="A") >> set.score(key="B")>> set.score(key="C") >> set.score(key="D") >> set.score(key="E");
	cout << "ABCDE" << endl;
	for (int i = 0; i < 5; i++) {


