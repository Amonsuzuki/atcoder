#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
	int r, ans1 = 1, ans2 = 0, ans4 = 0;
	cin >> r;
	double base = 0.5;
	for(double i=0; i<n && r*r - (i+1+base)*(i+1+base) - 0.5 * 0.5>0; i++) {
		if (i=0)
			double tmp = r*r - (i+1+base)*(i+1+base)

		if (r * r - (i+1+base)*(i+1+base)

		ans += 
	}
	cout << ans1 + ans2 * 2 + ans4 * 4 << endl;
}
